# Service que expone el microservicio dentro del cluster (IE2)
apiVersion: v1
kind: Service
metadata:
  name: microservicio-ep3
  labels:
    app: microservicio-ep3
spec:
  selector:
    app: microservicio-ep3
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
